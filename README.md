# Homebrew Tap

Homebrew tap for [OnAir](https://github.com/chaiovercode/on-air) — a macOS menu bar app that keeps your meetings always visible.

## Install

```bash
brew tap chaiovercode/tap
brew install --cask on-air
```

## Update

```bash
brew upgrade --cask on-air
```
